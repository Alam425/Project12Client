import { Link } from "react-router-dom";

const ClassesList = ({i}) => {
    return (
        <div className="py-2 px-10">
            <p className="">{i}</p>
        </div>
    );
};

export default ClassesList;